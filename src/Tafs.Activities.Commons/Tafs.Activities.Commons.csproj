<Project Sdk="Remora.Sdk">

  <PropertyGroup>
    <LibraryFrameworks>net461;netstandard2.1;net6.0;net7.0</LibraryFrameworks>
  </PropertyGroup>

  <PropertyGroup>
    <VersionPrefix>0.2.0</VersionPrefix>
    <PackageReleaseNotes>Add String Extensions</PackageReleaseNotes>
    <Description>
      Provides a set of common extensions and other unsorted code.
      
      New in $(VersionPrefix)$(VersionSuffix): $(PackageReleaseNotes)
    </Description>
    <PackageTags>$(PackageTags)</PackageTags>
  </PropertyGroup>

</Project>
